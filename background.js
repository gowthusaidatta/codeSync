chrome.action.onClicked.addListener((tab) => {
  // This is a placeholder. 
  // The content script injects the UI automatically.
  // You could add logic here to programmatically toggle the UI if needed.
  console.log("Pandu AI icon clicked.");
});
